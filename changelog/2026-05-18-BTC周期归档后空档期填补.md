# CHANGELOG

## 2026-05-18

- **`tasks/daily-report-stage4.md` — 归档后空档期填补** — BTC周期归档后，若距离下次日报定时触发（09:00 / 21:00 GMT+8）超过1小时，则创建临时cron 10分钟后拉起新周期。新增 `A.4.3` 步骤，通过 cron(list) 查询 `july-btc-morning-v2` / `july-btc-evening-v2` 的 `state.nextRunAtMs`，间隔 > 3600s 或任务不存在时触发；间隔 ≤ 1h 则跳过。临时任务 `deleteAfterRun: true`，一次后自动清除。
