# 日报任务 - 阶段四：警报管理

此任务为日报工作流的第四阶段，负责警报规则的全生命周期管理。

---

## 触发方式

- 由阶段三结束后触发
- 接收标准化的周期状态信息

---

## 日志文件

**所有阶段共用同一个日报进程日志文件：**

路径：`logs/daily-report-process.log`

格式：追加模式，记录阶段四的开始、结束、警告、错误。

**异常标识规则：**

| 级别 | 标识 | 含义 |
|------|------|------|
| **警告** | `⚠️ WARN` | 不影响流程继续执行 |
| **错误** | `⛔ ERROR` | 可能影响后续阶段 |

---

## 执行步骤

### 步骤1: 解析上一步消息或定位默认路径

**⚠️ 优先从上一步消息解析参数，若解析失败则从本地默认路径查找（保底措施）。**

#### 1.1 从上一步消息解析参数

预期上一步消息格式：
```
阶段三仓位管理已完成。
周期状态: [所有仓位平仓，已完成归档/周期活跃中]
周期路径: [已归档 | active/cycle-xxx]
请读取 tasks/daily-report-stage4.md 开始阶段四警报管理。
```

**提取关键参数：**

| 参数 | 消息中的标记 | 用途 |
|------|-------------|------|
| 周期状态 | `周期状态:` | 决定执行逻辑（清零 vs 正常管理） |
| 周期路径 | `周期路径:` | 定位报告文件（仅 active 状态需要） |

**说明：** 持仓状态、最近平仓不影响阶段四流程，不传递。

#### 1.2 保底措施：从本地默认路径查找

**如果上一步消息解析失败，执行保底查找：**

```bash
# 尝试查找活跃周期
CYCLE_ACTIVE=$(ls -td active/cycle-* 2>/dev/null | head -1)
```

**保底日志记录：**
```
[$NOW] [阶段四] ⚠️ WARN: 上一步消息解析失败，使用保底路径查找
[$NOW] [阶段四] 保定路径: 周期=${CYCLE_DIR}, 状态=${CYCLE_STATUS}
```

#### 1.3 确认路径有效性

| 路径类型 | 确认方式 | 失败处理 |
|---------|---------|---------|
| 周期目录 | `ls -d $CYCLE_DIR` | ⛔ ERROR，无法继续 |
| 日报文件（active状态） | `test -f $REPORT_FILE` | ⚠️ WARN，跳过报告读取 |

**注意：** archived 状态下不需要日报文件（直接执行警报清零）。

**日志记录：**
```
[$NOW] [阶段四] 参数来源: [消息解析 | 保底查找]
[$NOW] [阶段四] 周期状态: [active | archived]
[$NOW] [阶段四] 周期路径: [active/cycle-xxx | archived/cycle-xxx]
```

**日志记录（确认解析结果）：**
```
[$NOW] [阶段四] 消息解析: 成功 / ⚠️ 使用保底
```

---

### 步骤2: 记录阶段开始

```bash
NOW=$(date '+%Y-%m-%d %H:%M:%S')
echo "[$NOW] [阶段四] 开始执行 | 周期状态: [active | archived]" >> logs/daily-report-process.log
```

---

### 步骤3: 根据周期状态分支执行

---

## 【分支A：周期已归档 → 警报清零】

**如果周期状态为 `所有仓位平仓，已完成归档`，执行警报清零流程。**

### A.1 读取 BTC 活跃警报规则

```bash
ls -la skills/btc-alert/rules/20*.js 2>/dev/null
```

> ⚠️ BTC 规则以日期 `20xx-xx-xx-` 开头，用 `20*` 匹配只读 BTC 规则。山寨规则以币名开头（如 `DOGE-`），不会被匹配。

记录当前活跃规则数量。

### A.2 归档 BTC 规则

```bash
# 只移动 BTC 规则（日期前缀），不影响山寨币规则
mv skills/btc-alert/rules/20*.js skills/btc-alert/rules-archive/ 2>/dev/null
```

### A.3 记录清零日志

```bash
NOW=$(date '+%Y-%m-%d %H:%M:%S')
echo "[$NOW] [阶段四] 周期归档，BTC警报全部清零 | 归档规则数: X | 归档规则: [规则列表]" >> logs/daily-report-process.log
```

**清零完成确认日志：**
```
[$NOW] [阶段四] 警报清零完成: 归档规则 X 个 → rules-archive/
```

### A.4 记录阶段结束并退出

```bash
NOW=$(date '+%Y-%m-%d %H:%M:%S')
echo "[$NOW] [阶段四] 完成执行（警报清零）" >> logs/daily-report-process.log
echo "[$NOW] [阶段四] ========== 阶段四结束 ========== " >> logs/daily-report-process.log
```

**归档情况无需后续步骤，阶段四结束。**

---

## 【分支B：周期活跃 → 正常警报管理】

**如果周期状态为 `周期活跃中`，执行完整警报管理流程。**

---

### B.1 读取日报报告

**定位并读取日报文件：**

```bash
ls -t active/cycle-*/reports/btc-report-*.md 2>/dev/null | head -1
```

**通读整篇日报，理解分析结论和操作建议。**

日报包含完整的市场分析、关键位置、操作建议等信息，你需要从中提炼警报管理意图。

**日志记录：**
```
[$NOW] [阶段四] 报告读取完成
```

---

### B.2 读取 BTC 现有警报规则

```bash
ls -la skills/btc-alert/rules/20*.js 2>/dev/null
```

> ⚠️ 只列出日期前缀的 BTC 规则（`20*`），不包含山寨币规则。

对每个规则文件了解：
- 规则名称
- 警报类型（上方价格/下方价格/非价格）
- 目标价位或触发条件
- `lifetime()` 状态

**日志记录：**
```
[$NOW] [阶段四] 现有规则: X 个 | [规则名称列表]
```

---

### B.3 归档失效规则

**先清理失效规则，再规划新警报。**

**判断失效条件：**
以下为举例，可根据实际情况灵活归档

| 失效情况 | 判断依据 |
|---------|---------|
| 价格位已失效 | 报告显示支撑/阻力已突破/跌破，或已无意义 |
| lifetime 已过期 | 规则自身判断过期 |
| 分析结论改变 | 报告趋势判断与警报方向冲突 |
| 入场条件已执行 | 阶段三已执行「立即入场」操作 |


**归档操作：**

```bash
mv skills/btc-alert/rules/<rule-name>.js skills/btc-alert/rules-archive/
```

**日志记录：**
```
[$NOW] [阶段四] 归档失效规则: xxx.js | 原因: [失效原因]
```

---

### B.4 罗列所有有价值的警报候选

**这个步骤是「发散」阶段：通读日报，尽可能收集所有值得设立的警报。**

**从日报中提取三类警报候选：**

| 警报类型 | 说明 |
|---------|------|
| **上方价格警报** | 阻力位突破、入场触发价格（向上方向） |
| **下方价格警报** | 支撑位跌破、入场触发价格（向下方向） |
| **非价格警报** | 交易量、波动率、持仓量、多空比、资金费率等 |

**不要局限于固定来源，而是从整篇报告中理解并提炼。**

日报的多个部分都可能提供警报线索：
- 价格行为分析 → 趋势判断、关键位置
- 行情推断 → 支撑/阻力位、值得关注的行为
- 仓位操作建议 → 入场条件、触发价格

**示例输出（候选列表）：**

```
候选警报列表（共 N 个）：

上方价格警报候选：
  - xxx
  - xxx

下方价格警报候选：
  - xxx
  - xxx

非价格警报候选：
  - xxx
  - xxx
```

**⚠️ 何时设定非价格类警报？**

每次警报管理时，必须主动评估是否需要设定此类警报。判断依据：

| 市场现象 | 可能的警报类型 |
|---------|---------------|
| 交易量异常放大或萎缩 | 交易量异动警报 |
| 价格在窄幅区间长时间震荡 | 波动率收窄警报（可能预示突破） |
| 短时间内剧烈波动 | 振幅警报 |
| 连续多根同向K线 | 趋势加速警报 |
| 持仓量OI快速增加或减少 | 持仓量变化警报 |
| 多空比发生明显反转 | 多空比反转警报 |
| Taker买卖比偏离常态 | Taker买卖比警报 |
| 资金费率异常（过高或过低） | 资金费率警报 |
| ... | **发散思考，不限于上述示例** |

**⚠️ 重要：发散思维**

以上仅为示例，**切勿局限于这些场景**。每次分析时，应主动思考：

- 当前市场有什么**异常或值得关注的现象**？
- 这些现象如果**持续或反转**，会对持仓产生什么影响？
- 是否有**其他维度**的数据可以提前预警？

**警示**：不要每次只设定价格警报。如果市场处于低波动期，价格警报可能长期不触发，但交易量、波动率的变化可能在提前发出信号。忽略这些信号可能导致错过关键转折点。

**如果候选列表中只有价格警报，说明思维已固化，需要主动打破。**

**日志记录：**
```
[$NOW] [阶段四] 警报候选: 上方价格 X 个 / 下方价格 X 个 / 非价格 X 个 / 共计 X 个
```

---

### B.5 筛选警报（符合限制）

**这个步骤是「收敛」阶段：从候选中选出最有价值的警报。**

**警报数量限制：**

| 类型 | 最大数量 | 说明 |
|------|---------|------|
| **价格警报** | **≤6 个价位** | 单个规则文件可包含多个价位（上方/下方不限） |
| 非价格警报 | **≤2 个** | 独立规则文件 |
| **总计** | **≤3 个规则文件** | 1个多价位规则 + ≤2个非价格规则 |

**筛选原则（价位）：**

| 优先级 | 选择依据 | 说明 |
|-------|---------|------|
| **高** | 触发后需要执行操作的价位 | 入场触发价、止损/止盈位 |
| **中** | 报告明确指出的关键支撑/阻力 | 技术分析关键位置 |
| **低** | 整数关口、斐波那契等参考价位 | 心理关口，有参考价值 |

**⚠️ 关键变化：**

- **废弃**「上方1个/下方1个」的筛选限制
- **改为**「最有价值的 ≤6 个价位打包进一个规则文件」
- 单次触发可传递组合信息（多个价位同时被触发）

**示例筛选**（当前价格 $78,500）：

```
候选价位（共8个）：
  上方：$79,443 (旗形顶部)、$80,000 (整数关口)、$81,500 (前高)、$82,000 (心理压力)
  下方：$77,500 (关键支撑)、$74,980 (情景C目标)、$73,596 (深度支撑)、$72,000 (整数关口)

筛选逻辑：
  ✅ $79,443 - 触发做多B（高优先级）
  ✅ $77,500 - 触发情景C（高优先级）
  ✅ $74,980 - 止盈/目标位（中优先级）
  ✅ $81,500 - 趋势反转点（中优先级）
  ✅ $80,000 - 心理关口（低优先级）
  ✅ $73,596 - 深度支撑（中优先级）
  
  ❌ $82,000 - 距离太远（$3,500），放弃
  ❌ $72,000 - 距离太远（$6,500），放弃

最终警报（打包为1个多价位规则）：
  - 多价位监控规则（6个价位）
  - 非价格规则1（如交易量异动）
  - 非价格规则2（如持仓量变化）
```

**日志记录：**
```
[$NOW] [阶段四] 筛选结果: 价格价位 X 个 / 非价格 X 个 / 共计 X 个规则文件
[$NOW] [阶段四] 最终价位列表: [价位及描述]
[$NOW] [阶段四] 最终非价格列表: [警报描述]
```

---

### B.5.5 分配价位确认策略（防假突破）

**⚠️ 必做步骤：为每个入选价位分配 `confirmPolicy` 和 `confirmMs`。**

筛选出最终价位列表后，不能直接创建规则——必须先为每个价位决定确认策略。这是防止假突破的关键。

**确认策略速查表：**

| 价位类型 | 推荐策略 | `confirmMs` | 理由 |
|---------|---------|------------|------|
| 止损位 (SL) | `instant` | 0 | 最后防线，不能延迟 |
| 止盈位 (TP1/TP2) | `touch` | 3-5 min | 到了就止盈，短暂确认即可 |
| 入场触发位（开仓/加仓） | `hold` | 15-20 min | **假突破高发区**，必须等待站稳 |
| 关键支撑/阻力（有 action） | `hold` | 10-20 min | 突破/跌破确认后才有操作意义 |
| 整数关口（心理位） | `deep_hold` | 20-30 min | 容易被假突破，且操作性弱 |
| 远处观测位（前高/前低等） | `deep_hold` | 20-30 min | 不急，长确认后再分析 |

**分配决策流程：**

```
对每个最终价位，问自己：
1. 这个价位被"摸一下"的概率有多大？（整数关口 > 结构位 > TP/SL）
2. 如果假突破触发了分析，会有什么后果？（入场误判 > 观测误判）
3. 触发后需要立即行动吗？（SL必须立即，入场信号需要确认）

→ 选择对应的 confirmPolicy
```

**反例（必须避免）：**

| 价位 | ❌ 错误策略 | 问题 | ✅ 正确策略 |
|------|-----------|------|------------|
| 入场触发 $77,300 | `instant` | 影线一碰就触发入场分析 → 假突破导致误入场 | `hold` (15-20min) |
| 整数关口 $80,000 | `touch` | 整数关口假突破极频繁，3分钟不够 | `deep_hold` (20-30min) |
| 止损位 $76,688 | `hold` | 止损到了必须立即触发，不能等20分钟 | `instant` |
| 14d前高 $79,490 | `touch` | 远处价格不急，触发也没有立即操作 | `deep_hold` (20-30min) |

**输出示例：**

```
最终价位及确认策略（共6个）：
  ⬆️ $77,304 (BB中轨/多头确认) → hold, 20min   [入场触发位，防假突破]
  ⬆️ $77,858 (TP1)              → touch, 3min    [止盈位，快确认]
  ⬆️ $78,500 (TP2)              → touch, 3min    [止盈位，快确认]
  ⬇️ $77,050 (多头防线)          → hold, 15min    [结构位，需站稳]
  ⬇️ $76,800 (结构弱化)          → hold, 15min    [结构位，需站稳]
  ⬇️ $76,688 (SL止损位)          → instant, 0min  [止损位，不能延迟]
```

**日志记录：**
```
[$NOW] [阶段四] 价位确认策略已分配 | [价位1: policy, ms] | [价位2: policy, ms] | ...
```

---

### B.6 执行警报管理

**对比现有规则 vs 最终列表，执行增删改。**

**对比逻辑：**

| 对比结果 | 操作 |
|---------|------|
| 现有规则在最终列表中 | 保持不变 |
| 现有规则不在最终列表中 | 归档 |
| 最终列表中有新警报 | 创建规则文件 |

**创建警报规则时：**

**查看`tasks/set-alert.md`进行警报设立：**

读取 `tasks/set-alert.md`，按其规范创建警报规则文件。

set-alert.md 提供完整的警报规范：
- 规则文件结构（name, interval, check, collect, trigger, lifetime）
- 冷却机制（必须 ≥ 1 小时）
- 异步触发（必须使用 spawn）
- 规则文件命名：`YYYY-MM-DD-<类型>-<描述>.js`
- 可用数据源和 API endpoint

**日志记录：**
```
[$NOW] [阶段四] 保持规则: xxx.js
[$NOW] [阶段四] 归档规则: xxx.js | 原因: 不在最终列表
[$NOW] [阶段四] 创建规则: xxx.js | 目标: xxx
```

**📋 API 诉求检查点（每次创建规则时必须检查）：**

创建规则过程中，如果发现 `api.js` 缺少所需的数据获取方法，必须：
1. 在规则代码中用 `api.fetch(url)` 临时实现
2. 在 `check()` / `collect()` 日志中输出 `[📋API诉求]` 标记
3. 向 `skills/btc-market-lite/API_REQUESTS.md` 追加诉求条目
4. 在流程日志中记录：
```
[$NOW] [阶段四] [📋API诉求] 需要封装: xxx | 已记录到 API_REQUESTS.md
```

如果没有 API 诉求，在汇总日志中明确标注：
```
[$NOW] [阶段四] [📋API诉求检查] 无新诉求
```

**警报管理完成汇总日志：**
```
[$NOW] [阶段四] 警报管理完成 | 活跃规则: X 个 | 归档规则: X 个 | 本次创建: X 个 | API诉求: X 条
```

---

### B.7 记录阶段结束

```bash
NOW=$(date '+%Y-%m-%d %H:%M:%S')
echo "[$NOW] [阶段四] 完成执行" >> logs/daily-report-process.log
echo "[$NOW] [阶段四] ========== 阶段四结束 ========== " >> logs/daily-report-process.log
echo "[$NOW] ========== 日报流程结束 ========== " >> logs/daily-report-process.log
```

---

## 异常处理

| 异常类型 | 级别 | 处理方式 |
|---------|------|---------|
| 报告文件读取失败 | `⛔ ERROR` | 记录异常，无法继续执行 |
| 警报规则文件创建失败 | `⚠️ WARN` | 记录异常，继续执行其他警报 |
| 规则归档移动失败 | `⚠️ WARN` | 记录异常，继续执行 |
| 现有规则读取失败 | `⚠️ WARN` | 假定为空，继续执行 |

---

## 文件存储规则

| 文件类型 | 路径 | 说明 |
|---------|------|------|
| 警报规则 | `skills/btc-alert/rules/20*.js` | 活跃规则（日期前缀区分 BTC） |
| 归档规则 | `skills/btc-alert/rules-archive/*.js` | 归档规则 |
| 日报进程日志 | `logs/daily-report-process.log` | 所有阶段共用 |

---

## 核心要求

1. **⭐ 优先解析上一步消息**：从阶段三结束后传递的消息中获取周期状态和路径
2. **保底措施必须执行**：上一步消息解析失败时，从本地默认路径查找
3. **周期状态决定流程**：archived → 清零，active → 正常管理
4. **通读整篇日报**：理解分析逻辑，提炼警报意图（仅 active 状态）
5. **归档失效规则前置**：先清理，再规划（仅 active 状态）
6. **两步筛选**：发散罗列候选 → 收敛筛选最终（仅 active 状态）
7. **遵守数量限制**：价格 ≤6 价位打包、非价格 ≤2 个、总计 ≤3 个规则文件
8. **发散思维**：候选阶段必须主动思考非价格维度
9. **禁止 FGI 触发**：恐惧贪婪指数不适合分钟级警报
10. **使用连续数据**：K线区间而非瞬时价格（见 set-alert.md 规范）
11. **触发模型从配置读取**：读取 `tasks/global-config.json` → `trigger.model`，`trigger()` 的 `cron add` 必须包含 `--model <该值>`
12. **日志记录完整**：所有操作记录到 daily-report-process.log

---

## 上一步消息规范

**阶段三结束后的上一步消息只需：**

| 参数 | 必须性 | 示例 |
|------|-------|------|
| 周期状态 | ✅ 必须 | `active` 或 `archived` |
| 周期路径 | ✅ 必须 | `active/cycle-xxx` 或 `archived/cycle-xxx` |

**不传递的参数：**
- 持仓状态、最近平仓 → 不影响阶段四流程

**缺失影响：**
- 缺少周期状态 → 无法决定清零还是正常管理（保底可从路径推断）
- 缺少周期路径 → 无法定位报告文件（保底可查找周期目录）

---

## 保底措施说明

**保底触发条件：**
- 上一步消息格式异常（无法解析）
- 上一步消息缺少关键参数

**保底查找规则：**

| 参数 | 查找逻辑 |
|------|---------|
| 活跃周期 | `ls -td active/cycle-* 2>/dev/null | head -1` |
| 归档周期 | `ls -td archived/cycle-* 2>/dev/null | head -1` |
| 周期状态 | 有活跃周期 → `active`；有归档周期 → `archived` |
| 日报文件 | `${CYCLE_DIR}/reports/btc-report-*.md`（仅 active 状态需要） |

**保底日志记录：**
- 必须记录 `⚠️ WARN: 上一步消息解析失败，使用保底路径查找`
- 必须记录保底推断的周期状态

---

## ⚠️ 执行前自查清单

**在完成警报管理后，必须逐项核对：**

### 数量限制核对

| 检查项 | 要求 | 易错点 |
|-------|------|-------|
| ☐ 价格警报价位是否 ≤ 6 个？ | 选最有价值的 ≤6 个价位 | 超过限制 |
| ☐ 非价格警报是否 ≤ 2 个？ | 选最值得关注的 ≤2 个 | 超过限制 |
| ☐ 总计规则文件是否 ≤ 3 个？ | 1个多价位 + ≤2个非价格 | 规则过多 |

### 思维发散核对

| 检查项 | 正确做法 | 易错点 |
|-------|---------|-------|
| ☐ 候选阶段是否有发散思维？ | 主动评估交易量、波动率等维度 | 只盯着价格 |
| ☐ 候选列表是否只有价格警报？ | 必须有非价格候选 | 思维固化 |
| ☐ 触发条件是否有数据源支持？ | 参考 set-alert.md 可用 API | 无法获取数据 |

### 延迟确认核对（防假突破）

| 检查项 | 正确做法 | 易错点 |
|-------|---------|-------|
| ☐ 每个价位是否分配了 confirmPolicy？ | 按价位类型选择 instant/touch/hold/deep_hold | 全部用默认或忽略 |
| ☐ 入场触发位是否用了 hold（非 instant）？ | 入场触发必须延迟确认 | 影线触发导致误入场 |
| ☐ SL/TP 是否用了合适的短延迟？ | SL=instant, TP=touch | SL 延迟导致止损过晚 |
| ☐ 整数关口/远处价是否用了 deep_hold？ | 远处价位不急确认 | 频繁触发无意义的分析 |
| ☐ 创建规则时用了延迟确认模板（set-alert.md §13）？ | 优先使用 §13 模板 | 使用 §12 无延迟模板 |

### 规则文件核对

| 检查项 | 正确做法 | 易错点 |
|-------|---------|-------|
| ☐ 冷却时间是否 ≥ 1 小时？ | COOLDOWN_MS = 60 * 60 * 1000 | 设定过短 |
| ☐ 是否使用异步触发？ | spawn 创建隔离会话 | 使用 execSync 阻塞 |
| ☐ lifetime 是否合理？ | 根据分析设定有效期 | 永久有效或立即过期 |
| ☐ 是否使用K线数据而非瞬时价格？ | getKlines获取区间高低价 | getTicker单点比较 |

### 禁止事项核对

| 检查项 | 正确做法 | 易错点 |
|-------|---------|-------|
| ☐ 是否使用了 FGI 触发？ | ❌ 禁止 | FGI 更新频率为日级 |

---

**⚠️ 核心理念提醒：**

> 先发散（罗列候选），再收敛（筛选最终）。候选阶段必须主动思考非价格维度，不要思维固化！
>
> **每个价位必须分配确认策略，否则就是给假突破开门。入场触发位用 hold（15-20min），止损位用 instant，整数关口用 deep_hold。**

---

阶段四-v4.24（延迟确认集成）